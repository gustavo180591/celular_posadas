---
const whatsappNumber = "5493764000000"; // <-- CAMBIÁ ACÁ A TU NÚMERO REAL
const mensajeGeneral = encodeURIComponent(
  "Hola, vi la página de celulares en Posadas y quiero ver los modelos disponibles."
);
const whatsappUrl = `https://wa.me/${whatsappNumber}?text=${mensajeGeneral}`;
---

<section class="relative isolate overflow-hidden bg-slate-950">
  <!-- Contenido del Hero -->
</section>
